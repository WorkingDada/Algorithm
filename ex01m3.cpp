#include <iostream>
#include <vector>
using namespace std;


int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    int n,a,i;
    vector<int> c;
    cin >> n >> a;
    while(n--){
        cin >> i;
        c.push_back(i);
    }
    while(a--){
        cin >> i;
        
    }
}